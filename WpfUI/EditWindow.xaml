<Window x:Class="WpfUI.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редагування"
        Height="250"
        Width="300">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="txtName"
                 Grid.Row="0"
                 Margin="5"
                 Height="30"/>

        <TextBox x:Name="txtResolution"
                 Grid.Row="1"
                 Margin="5"
                 Height="30"/>

        <TextBox x:Name="txtSize"
                 Grid.Row="2"
                 Margin="5"
                 Height="30"/>

        <Button Grid.Row="3"
                Content="Зберегти"
                Height="35"
                Margin="5"
                Click="Save_Click"/>

    </Grid>

</Window>