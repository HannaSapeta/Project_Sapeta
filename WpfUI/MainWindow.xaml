<Window x:Class="WpfUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Media Manager"
        Height="521"
        Width="931">

    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="2"
                   Text="Менеджер медіафайлів"
                   FontSize="24"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- Бічна панель -->
        <StackPanel Grid.Row="1"
                    Grid.Column="0"
                    Margin="10">

            <Button Content="Додати"
                    Click="Add_Click"/>

            <Button Content="Редагувати"
                    Click="Edit_Click"/>

            <Button Content="Видалити"
                    Click="Delete_Click"/>

        </StackPanel>

        <!-- Робоча область -->
        <DataGrid x:Name="mediaGrid"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="10"
                  AutoGenerateColumns="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Назва"
                                    Binding="{Binding FileName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Розмір"
                                    Binding="{Binding FileSizeMb}"
                                    Width="*"/>

                <DataGridTextColumn Header="Роздільність"
                                    Binding="{Binding Resolution}"
                                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>
